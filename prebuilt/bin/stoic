#!/bin/bash
#set -x
set -euo pipefail

script_dir="$(dirname "$(readlink -f "$0")")"
stoic_release_dir="$(realpath "$script_dir/..")"
java -jar "$stoic_release_dir"/jar/hostMain.jar "$stoic_release_dir" "$@"
